// Declaration of the `Component` class, which has a constructor to initialize `name` and `price`.
class Component {
    constructor(name, price) {
        this.name = name;
        this.price = price;
    }
}
