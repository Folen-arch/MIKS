// Declaration of class `Capacitor`, which inherits class `Component` and has a constructor to set `price`.
class Capacitor extends Component {
    constructor(price) {
        super("Capacitor", price);
    }
}
