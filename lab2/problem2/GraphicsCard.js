class GraphicsCard {
    constructor(model) {
        // This is the constructor method. It is called when a new instance of GraphicsCard is created.
        // It takes a 'model' parameter and assigns it to the 'model' property of the instance.
        this.model = model;
    }
}
