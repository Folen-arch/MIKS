class Memory {
    constructor(size) {
        // This is the constructor method. It is called when a new instance of Memory is created.
        // It takes a 'size' parameter and assigns it to the 'size' property of the instance.
        this.size = size;
    }
}
