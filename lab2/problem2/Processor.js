class Processor {
    constructor(model) {
        // This is the constructor method. It is called when a new instance of Processor is created.
        // It takes a 'model' parameter and assigns it to the 'model' property of the instance.
        this.model = model;
    }
}
